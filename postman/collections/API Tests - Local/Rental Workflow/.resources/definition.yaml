$kind: folder
name: Book Workflow
description: |-
  End-to-end workflow test: create book → add to cart → checkout → cleanup.
  Requests must run in order (01 → 02 → 03 → 04).
  Uses the workflowBookId collection variable to chain data between steps.
order: 5000
