$kind: folder
name: Rental Workflow
description: |-
  End-to-end workflow test: create vehicle → rent → return → cleanup.
  Requests must run in order (01 → 02 → 03 → 04).
  Uses the workflowVehicleId collection variable to chain data between steps.
order: 5000
